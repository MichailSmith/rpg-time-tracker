interface Node {
  id: ID!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

type Query {
  node(id: ID!): Node
  elapsedTime: Int!
}

type Mutation {
  advanceTime(by: Int!): Int!
  resetTime(to: Int!): Int!
}

